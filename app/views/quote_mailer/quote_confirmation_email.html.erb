<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Quote Request</h1>
    <p>Thank you for submitting your request, a representative will get back to you shortly.
    </p>
    <ul>
      <li>
        <% @quote.quote_fields.each do |field| %>
          <%= field.to_s + ": " + @quote.send(field) %>
        <% end %>
      </li>
    </ul>
  </body>
</html>