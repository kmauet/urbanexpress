<%= image_tag @vehicule_image.photo %>

<%= simple_form_for(@vehicule_image) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :vehicule_id %>
    <%= f.input :photo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
